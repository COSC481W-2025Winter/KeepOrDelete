const { expect } = require('chai');

describe('sample test', function () { //just a sample test to get used to syntax and format
    it('should be true', function () {
        expect(true).to.equal(true);
    });
});